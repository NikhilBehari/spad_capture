# Continuous 3x3_wide streaming with the live dashboard. Runs until
# Ctrl-C. Frames are saved to a single growing HDF5.

name: "streaming"

sensor:
  zone_mode: "3x3_wide"
  range_mode: "long"

capture:
  mode: "streaming"
  interval_s: 0.0

storage:
  format: "h5"

viz:
  enabled: true
